// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("aoq", "for aoq support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("aoq", "enable aoq support", "no");

if (PHP_AOQ != "no") {
	EXTENSION("aoq", "aoq.c");
}

